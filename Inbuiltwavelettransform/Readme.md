This MATLAB program applies a 2-D Discrete Wavelet Transform (DWT) using the Haar wavelet on a grayscale image. The image is decomposed into approximation, horizontal, vertical, and diagonal components and visualized using subplots.

